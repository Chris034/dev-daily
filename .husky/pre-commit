#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run linting and type checks before committing
npm run lint
npm run type-check

# If any of the above commands fail, prevent the commit
if [ $? -ne 0 ]; then
  echo "Commit failed due to linting or type-check errors."
  exit 1
fi