import React from 'react';

const SettingsPage: React.FC = () => {
  return (
    <section className="simple-page-card">
      <p className="hero-eyebrow">Settings</p>
      <h2>Profile controls are intentionally minimal</h2>
      <p>
        Preferences and account controls will be layered in after this design-first MVP.
      </p>
    </section>
  );
};

export default SettingsPage;